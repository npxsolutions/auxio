# Week 1–8 content — sweep status

**Source of truth:** `linkedin-posts.md` → `buffer-bulk-import.csv` is a snapshot.

## Pending sweeps before any post ships

Two separate sweeps are outstanding on posts 2–24:

1. **Rival-name sweep** — remove Feedonomics / Shopify Marketplace Connect / Linnworks by brand (per `project_current_state.md` memory and `feedback_no_rival_names.md`). Post #1 was done; posts 2–24 remain.
2. **App Store sweep (new, 2026-04-21)** — direct-install distribution only. See `linkedin-posts.md` and `x-threads.md` headers for the specific posts (3, 11, 21, 23, 24 + Threads 6, 8 + the Week-7 Friday row in the cadence table).

Do both sweeps in one pass — same lines, same voice, saves time.

## Buffer sync reminder

If `buffer-bulk-import.csv` has already been uploaded to Buffer, **delete the affected weeks from Buffer and re-upload after the sweep.** Hand-editing the CSV without regenerating from `linkedin-posts.md` will desync the two.

Affected rows in the CSV (current state):
- Line 42 — Post #3 narrative
- Line 90 — Post #11 product pitch
- Lines 331–348 — Post #21 (the dedicated App-Store-as-channel post)
- Lines 368–388 — Post #24 retro

All flow from `linkedin-posts.md` posts marked in its header warning.
